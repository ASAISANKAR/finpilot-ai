spring:
  application:
    name: demo

  datasource:
    url: jdbc:postgresql://pg-11f447e7-aratrin-375b.e.aivencloud.com:15912/finpilot?sslmode=require
    username: ${USER_NAME}
    password: ${PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

server:
  port: 8080